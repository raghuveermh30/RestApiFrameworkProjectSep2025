package com.qa.api.constants;

public enum AuthType {

    //Enum is the collection of multiple constants
    //Enum variables will prefer with UpperCase

    BEARER_TOKEN,
    OAUTH2,
    BASIC_AUTH,
    API_KEY,
    NO_AUTH
}
